name: Python package

on:
  push:
    branches:
      - test

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Dependancies
      run: pip install -r requirements.txt

    - name: Test with pytest
      run: pytest Model_testing.py
